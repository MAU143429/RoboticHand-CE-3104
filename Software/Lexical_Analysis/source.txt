fn main(){
    let res = false;
    let num = 49;
    Println!("Hola", res);
    Move(["A", "T", "M"], res);
    procedimiento();
}

fn procedimiento() {
    let var = true;
    for cifra in 6..7 {
        let num = cifra;
        let newNum = Opera(+, num, Opera(+, Opera(+, num, 4), 4));
    }
}