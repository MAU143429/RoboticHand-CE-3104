fn main(){
    let res = false;
    let num = 49;
    Println!("Hola", res);
    Move(["A", "T", "M"], res);
    let cifra2 = false;
    procedimiento(cifra2, num);
}

fn procedimiento(cifra1, cifra3) {
    let var = true;
    for cifra in 6..7 {
        let num = cifra;
        let newNum = Opera(+, num, Opera(+, Opera(+, num, 4), 4));
    }
}