fn main(){
    let res = false;
    let sum = funcion(65, true);
    let num = 49;
    Println!("Hola", res);
    Move(["A", "T", "M"], res);
    let cifra2 = false;
    procedimiento();
}

fn funcion(rere, tun) -> integer{
    let var53 = 54;
    return tun;
}

fn procedimiento() {
    let var = true;
    for cifra in 6..7 {
        let num = cifra;
        let newNum = Opera(+, num, Opera(+, Opera(+, num, 4), 4));
    }
}