fn main(){
    let res = false;
    Println!("Hola", res);
    Move("T", res);
    if true == res {
        let sum = true;
        let hola = 49;
    }
    elseif res == true{
        let res = true;
        let div = 67;
    }
    else {
        if res == false{
            Delay(2000, "Mil");
        }
    }
    Move(["A", "T", "M"], res);
}